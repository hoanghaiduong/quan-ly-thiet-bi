export enum FileTypes {
    IMAGE = 'IMAGE',
    EXCEL = 'EXCEL',
    OTHER = 'OTHER',
}
export enum ImageTypes {
    CARD_USER = 'users',
    CARD_DEVICE = 'devices',
    CARD_DEVICE_DETAIL = 'device_details',
}
